/**
 * WiFi and MQTT Configuration Example
 *
 * Copy this file to config.h and fill in your credentials.
 * config.h is gitignored to protect your credentials.
 */

#ifndef CONFIG_H
#define CONFIG_H

// WiFi Configuration
const char* WIFI_SSID = "YOUR_WIFI_SSID";         // Your home WiFi SSID (2.4GHz!)
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD"; // WiFi password

// MQTT Broker Configuration
// FOR DEVELOPMENT: Use your PC's IP (find with: ip addr)
// FOR PRESENTATION: Use 192.168.137.100 (RPI in hotspot)
const char* MQTT_BROKER_IP = "192.168.1.XXX";  // Your PC IP
const int MQTT_PORT = 1883;

#endif
